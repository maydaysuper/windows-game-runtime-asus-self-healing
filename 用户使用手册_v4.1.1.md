# v4.1.1 用户使用方式

v4.1.0 能打开窗口。v4.1.1 修 Dry Run 跳转、运行库空 WARN、中文排版，并在退出时写使用日志。

## 安装

1. 先卸载全部 3.4.x（设置 → 应用）。已装 4.1.0 可直接覆盖安装 4.1.1。
2. 安装 `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v4.1.1_x64.exe`。
3. 从开始菜单打开「Windows Game Runtime - ASUS Armoury Self-Healing Center」。

Portable 版解压后运行同一目录里的 `WindowsGameRuntimeASUSSelfHealing.WinUI.exe`，不要单独拷走 EXE。旁边必须有 `Backend` 文件夹和 `wpfgfx_cor3.dll`。

安装不需要管理员。只有执行 ASUS / 运行库 / 安全 GPU 修复时才会弹 UAC。

## 五个入口

1. **系统健康**：重新体检，看 ASUS、运行库、崩溃、安全四块。
2. **ASUS 奥创中心**：检测并生成方案。只有 Eligible 才执行安全修复。
3. **游戏运行库**：联网对比 → Dry Run → 有资格再修 VC++ / DirectX。官方包卡片没取到安装器时会显示 INFO，不代表本机运行库坏了。
4. **崩溃 / Dump**：GPU / ReBAR 深度诊断。不会自动 DDU，不会改 BIOS。
5. **报告中心**：系统报告、修复报告、最终验收、诊断 ZIP、重启后续跑。

## 使用日志

关闭程序后会生成：

`%LocalAppData%\WindowsGameRuntimeASUSSelfHealing\Logs\session-日期时间.log`

同一目录的 `session-latest.log` 是最近一次。里面会记下本次捕获到的 bug。没有故障时也会写一份结束摘要。

## 不要做的事

- 不要再装 3.4.x
- 不要双击源码 publish 目录里的孤立 EXE
- 不要手动改 `Backend` 里的脚本
