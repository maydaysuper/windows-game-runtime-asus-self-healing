# v3.4.5 使用补充

正常运行 `Launch-Win11.cmd`。构建器会自动检查静态门禁、准备 .NET 10 SDK、Restore 并发布 WinUI 3。

如果发布仍失败，请发送 `BuildLogs/WinUI3_OneClick_*.log` 与 `BuildLogs/Publish_*.log`；若需要精确 MSBuild 图，再附 `Publish_*.binlog`。
