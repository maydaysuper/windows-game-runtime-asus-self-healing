# v4.4.0

检测 C++ 运行库时看真实文件，不再只看注册表。和官方不一样或文件不完整就修，修完立刻再验一次。

- 检查完整的 C++ 运行库文件，不只看两三个。
- 用本机文件版本和官方版本对比。文件版本不一致、加载失败，都按需要修复处理。
- 联网对比官方版本改为并行查询，避免重复检测拖时间。没网时用最近一次官方结果。
- 修复后立刻再看本机文件是否齐全、是否已经到官方版本。一种方法没修好会换下一种。
- 安装包缓存只在有限范围内查找，避免扫描卡死。
