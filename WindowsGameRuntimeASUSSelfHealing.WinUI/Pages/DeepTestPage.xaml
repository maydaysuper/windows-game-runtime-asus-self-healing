<Page
    x:Class="WindowsGameRuntimeASUSSelfHealing.WinUI.Pages.DeepTestPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:WindowsGameRuntimeASUSSelfHealing.WinUI.Controls"
    xmlns:models="clr-namespace:WindowsGameRuntimeASUSSelfHealing.WinUI.Models">
    <Grid Margin="26">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="深度检查" FontSize="22" FontWeight="SemiBold"/><TextBlock Text="VC++ LoadLibrary、DXGI/D3D11/D3D12、GPU/PnP、.NET、WebView2、Vulkan、OpenAL、XNA、崩溃证据" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0"/></StackPanel>
            <Button x:Name="RunButton" Grid.Column="1" Content="开始后台深度测试" Click="RunButton_Click"/>
        </Grid>
        <ctrl:StatusBanner x:Name="DeepInfo" Grid.Row="1" Margin="0,18,0,0" IsOpen="True" Title="按需测试" Message="耗时 API/事件日志/PnP 查询在独立 PowerShell 进程执行，界面主线程不会等待。"/>
        <ListView x:Name="DeepList" Grid.Row="2" Margin="0,18,0,0" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:DeepTestItem}">
                    <Border CornerRadius="10" Padding="12" Margin="0,0,0,7" Background="{Binding StatusBrush}">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="1.2*"/><ColumnDefinition Width="1.5*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="4*"/></Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Category}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Test}"/>
                            <TextBlock Grid.Column="2" Text="{Binding Status}" Foreground="{Binding StatusForeground}" FontWeight="SemiBold"/>
                            <TextBlock Grid.Column="3" Text="{Binding Detail}" TextWrapping="Wrap"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
