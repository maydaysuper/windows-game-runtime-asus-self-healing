<Page x:Class="WindowsGameRuntimeASUSSelfHealing.WinUI.Pages.IdentityPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="using:WindowsGameRuntimeASUSSelfHealing.WinUI.Controls">
    <Grid Padding="26"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel><TextBlock Text="组件识别 / 故障关联" FontSize="22" FontWeight="SemiBold"/><TextBlock Text="ProfileID + ProductCode + UpgradeCode + Burn + Signer + SHA256 + 版本；关联评分只归组证据，不授权修复" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0"/></StackPanel>
            <Button x:Name="RefreshButton" Grid.Column="1" Content="后台刷新" Click="RefreshButton_Click"/>
        </Grid>
        <ctrl:StatusBanner x:Name="Info" Grid.Row="1" Margin="0,18,0,0" IsOpen="True" Title="按需加载" Message="签名/哈希/MSI/Burn 指纹在独立进程计算。"/>
        <TextBox x:Name="Output" Grid.Row="2" Margin="0,18,0,0" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" FontFamily="Consolas"/>
    </Grid>
</Page>
