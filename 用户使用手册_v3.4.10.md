# v3.4.10 用户使用方式

v3.4.9 安装后双击没窗口，这是打包问题，不是你的电脑问题。请改用 **v3.4.10**。

普通用户不要自己编译。打开 GitHub Release，下载：

- `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v3.4.10_x64.exe`（推荐）
- 或 `Windows_Game_Runtime_ASUS_SelfHealing_Portable_v3.4.10_win-x64.zip`

Setup 按当前用户安装，不需要管理员。真正执行 ASUS / 运行库 / GPU 安全修复时才会弹 UAC。

安装目录里除了主程序 EXE，必须能看到一堆 DLL（至少包含 `Microsoft.ui.xaml.dll`）。如果只有一个超大 EXE，说明还是 v3.4.9，不要用。

如果仍然打不开，看：

`%LOCALAPPDATA%\WindowsGameRuntimeASUSSelfHealing\Logs\startup-crash.log`

火绒 / Windows SmartScreen 可能拦截未签名程序。把安装目录加入信任后重试。

卸载程序时不会删除 `%LOCALAPPDATA%\WindowsGameRuntimeASUSSelfHealing\State` 里的 Transaction / Incident / Workflow 历史。
