# v3.4.11 用户使用方式

v3.4.10 双击后弹出「发生了快速异常检测失败」是 Windows App SDK Bootstrap 在 Main 之前 Fail Fast，不是你的电脑问题。请改用 **v3.4.11**。

普通用户不要自己编译。打开 GitHub Release，下载：

- `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v3.4.11_x64.exe`（推荐）
- 或 `Windows_Game_Runtime_ASUS_SelfHealing_Portable_v3.4.11_win-x64.zip`

不要双击本地 `publish` / 编译输出目录里的 EXE。那种目录往往只有几个托管 DLL，没有 `Microsoft.ui.xaml.dll`，即使不 Fail Fast 也打不开。

Setup 按当前用户安装，不需要管理员。真正执行 ASUS / 运行库 / GPU 安全修复时才会弹 UAC。

安装目录里除了主程序 EXE，必须能看到一堆 DLL（至少包含 `Microsoft.ui.xaml.dll`）。从开始菜单或桌面快捷方式打开。

## 五个一级入口

1. **系统健康**：点「重新体检」。
2. **ASUS 奥创中心**：点「检测并生成方案」，只有 Eligible 才点「执行安全修复」。
3. **游戏运行库**：先「联网对比」，再 Dry Run，有资格才「安全修复」。
4. **崩溃 / Dump**：刷新证据 → GPU/ReBAR 深度诊断 → 需要时分析 Dump。
5. **报告中心**：最终验收、重启后续跑、诊断 ZIP。

如果仍然打不开，看：

`%LOCALAPPDATA%\WindowsGameRuntimeASUSSelfHealing\Logs\startup-crash.log`

火绒 / Windows SmartScreen 可能拦截未签名程序。把安装目录加入信任后重试。

卸载程序时不会删除 `%LOCALAPPDATA%\WindowsGameRuntimeASUSSelfHealing\State` 里的 Transaction / Incident / Workflow 历史。
