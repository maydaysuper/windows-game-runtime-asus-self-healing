# v4.2.0 用户使用方式

v4.2.0 是当前最终运行版。不从开始菜单启动。

## 安装版

1. 先卸载全部 3.4.x。已装 4.1.x 可直接覆盖安装 4.2.0。
2. 运行 `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v4.2.0_x64.exe`。
3. 双击桌面上的「自愈中心」。

## 便携版

1. 解压完整文件夹，不要只抽出 EXE。
2. 双击 `SelfHealingCenter.exe`。
3. 不要单独运行 `App` 目录里的程序。

## 五个入口

1. **系统健康**：重新体检。奥创那一格只回答有没有 4151/4152。
2. **ASUS 奥创中心**：只查 4151/4152 更新错误。有才生成方案并修复。
3. **游戏运行库**：看本机 VC++ / DirectX 是否可用。不再联网对比，也不再自动修运行库。
4. **崩溃 / Dump**：GPU / ReBAR 深度诊断。
5. **报告中心**：最终验收、诊断 ZIP、重启后续跑。

## 运行库异常时

请自行安装：

- Microsoft Visual C++ Redistributable（x64 与 x86）
- 如缺 D3DX/XInput：DirectX End-User Runtime

## 使用日志

关闭软件后：`%LocalAppData%\WindowsGameRuntimeASUSSelfHealing\Logs\session-latest.log`
