# v3.4.7 用户使用方式

普通用户不再需要安装 .NET SDK 或自己编译源码。

推荐使用 **Setup 安装版**：运行 `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v3.4.7_x64.exe`，完成后直接从开始菜单启动。安装程序本身按当前用户安装，不需要管理员权限；只有真正执行 ASUS、运行库或受允许的 GPU 修复时，Elevated Broker 才会弹 UAC。

也可以使用 **Portable 便携版**：完整解压 `Windows_Game_Runtime_ASUS_SelfHealing_Portable_v3.4.7_win-x64.zip` 后运行主程序。不要只复制主 EXE，因为 `Backend` 目录包含 hash-lock 修复链。

正式发布包同时提供 `RELEASE_SHA256.txt` 和 `RELEASE_MANIFEST.json` 用于完整性核对。
