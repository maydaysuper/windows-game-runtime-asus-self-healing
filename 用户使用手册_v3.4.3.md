# v3.4.3 简明使用说明

1. 将整个 ZIP 解压到普通文件夹，不要直接在压缩包内运行。
2. 双击 `Launch-Win11.cmd`。
3. 程序会先运行 PowerShell Parser / 架构 / Hash-Lock 静态门禁。
4. 门禁通过后检查/安装 .NET 10 SDK，然后 Restore、Publish 并启动 WinUI 3 程序。
5. 如果仍失败，请发送 `BuildLogs` 中同一时间戳的：
   - `WinUI3_OneClick_*.log`
   - `StaticTests_*.log`

v3.4.3 专门修复 Windows PowerShell 5.1 对 UTF-8 XAML/C# 源文件的错误解码问题；ASUS 4151/4152 核心修复链未改动。
