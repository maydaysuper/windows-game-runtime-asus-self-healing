# v4.1.2 用户使用方式

v4.1.1 能打开。v4.1.2 修滚动、官方包结论、顶栏遮挡、图标，并把奥创中心改成只查更新错误。

## 安装

1. 先卸载全部 3.4.x。已装 4.1.0 / 4.1.1 可直接覆盖安装 4.1.2。
2. 安装 `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v4.1.2_x64.exe`。
3. 从开始菜单打开「自愈中心」（盾牌十字图标）。

## 五个入口怎么分工

1. **系统健康**：总览。奥创那一格只回答「有没有更新错误」，细节不去重复检测。
2. **ASUS 奥创中心**：只查 Armoury Crate 现在有没有 4151 / 4152。有才能安全修复。
3. **游戏运行库**：本机 VC++ / DirectX 一张表；官方包另一张表，每张都写「不必修复」或「需要关注」。
4. **崩溃 / Dump**：GPU / ReBAR。不会自动 DDU，不会改 BIOS。
5. **报告中心**：报告、验收、诊断 ZIP、重启后续跑。

鼠标滚轮在运行库和奥创页会滚整页。

退出后使用日志仍在 `%LocalAppData%\WindowsGameRuntimeASUSSelfHealing\Logs\`。
