# v3.4.6 用户交付方式

正式用户不再需要自己安装 SDK 或运行构建脚本。发布流程提供两种成品：

1. **Setup 安装版**：下载 `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v3.4.6_x64.exe`，双击安装后直接运行。安装本身按当前用户写入 LocalAppData，不要求管理员权限；只有执行 ASUS/运行库/GPU 修复时 Elevated Broker 才会请求 UAC。
2. **Portable 便携版**：下载 `Windows_Game_Runtime_ASUS_SelfHealing_Portable_v3.4.6_win-x64.zip`，完整解压后直接运行主程序 EXE。不要只单独复制主 EXE，因为 Backend 目录属于 hash-lock 修复链的一部分。

开发用 `Launch-Win11.cmd` / `OneClick-Win11.ps1` 继续保留，但不再作为普通用户的推荐入口。
