# v3.4.1 简明使用手册

> v3.4.1 为 Windows 启动器兼容性热修复。推荐双击 `一键构建并启动_Win11.cmd`；如果系统对中文文件名关联异常，也可直接双击纯英文备用入口 `Launch-Win11.cmd`。两个入口内容完全一致，均为 ASCII-only + CRLF，不依赖 `chcp 65001`。


## 最常用流程

打开程序先看 **系统健康**。ASUS Armoury Crate / 4151 / 4152 问题进入 **ASUS 奥创中心**，先“检测并生成方案”，只有 Eligibility、Recipe、hash-lock 和安全门禁全部通过后才执行修复。ASUS 仍然是本软件的第一核心功能。

游戏黑屏、爆显存、驱动崩溃或怀疑 ReBAR 时进入 **崩溃 / GPU / ReBAR / Dump**，点击 **GPU / ReBAR 深度诊断**。程序会合并显存、Shared GPU Memory、ReBAR、TDR、厂商驱动事件、WHEA、Kernel-Power、LiveKernelReports 和最近 Dump，再给出根因方向与置信度。

如果修复要求重启，重启后进入 **报告中心** 点击“重启后续跑”。修复完成后可点击 **执行最终验收**，进入严格 Verification / Observation 流程。

## 四个功能板块

- **ASUS 奥创中心**：Armoury Crate / HAL / VGA / Holtek / ENE，重点处理已验证的 4151/4152 场景；未知新版本只诊断。
- **游戏运行库**：VC++ / DirectX 检测、官方包校验和安全修复。
- **崩溃 / GPU / ReBAR / Dump**：近期崩溃、Dedicated/Shared GPU Memory、ReBAR、TDR、`nvlddmkm`/AMD/Intel、WHEA/PCIe、Kernel-Power、WER/LiveKernel、本机 `.dmp` 分析；发现可访问的最新 LiveKernelReports dump 时会最佳努力自动解析一次。
- **报告中心**：系统报告、修复报告、Dump 报告、GPU/ReBAR 报告、完整诊断 ZIP、Transaction 历史、重启续跑、最终验收。

## GPU / ReBAR 结果怎么看

- **真爆显存 / 显存资源压力**：Dedicated 峰值逼近显存容量，同时 Shared GPU Memory 明显上升，或 Dump 出现 OOM 证据。先降低纹理、光追、分辨率、高显存 Mod/Overlay，再复现。
- **显卡驱动 / TDR 崩溃**：Display 4101、厂商驱动事件、LiveKernelEvent 或 Dump 指向 GPU reset/driver。此时才会出现“安全 GPU 修复”。
- **ReBAR 兼容性嫌疑**：ReBAR 有较强启用证据并出现 TDR，但没有显存饱和、PCIe WHEA 或异常掉电。程序不会自动关 ReBAR，需要在 BIOS 中做同场景 A/B 才能确认。
- **PCIe 链路 / 硬件错误**：WHEA/Root Port 证据优先。检查插槽、延长线/转接卡、BIOS/芯片组和超频/降压稳定性。
- **供电 / 瞬时掉电嫌疑**：重复 Kernel-Power 41 且无 BugCheck，并伴随 GPU/WHEA 证据。软件无法测 PSU 电压，所以只给“嫌疑”，不下百分百硬件结论。

## 安全 GPU 修复会做什么

只在 `DRIVER_TDR` 软件侧路径开放：

1. 将 DirectX / NVIDIA / AMD Shader Cache 移动到本地备份目录，再重建空缓存目录；被占用的缓存跳过，不强删。
2. 执行 Windows `pnputil /scan-devices` 重新扫描设备。
3. 完成后重新跑 GPU / ReBAR 诊断并生成新报告。

它**不会**自动 DDU、卸载/禁用/重启显卡、删除 DriverStore、修改 `TdrDelay/TdrDdiDelay/TdrLevel`、写 BIOS/UEFI/ReBAR、改电压/频率/功耗。

## 报告位置

报告中心会统一列出：系统健康报告、修复/Transaction 报告、Dump 报告、GPU/ReBAR 根因报告和完整诊断 ZIP。GPU 报告会保留分类、置信度、ReBAR 证据、显存峰值、事件摘要、Dump 关联证据和建议。

## 重要安全说明

未知 ASUS 新版本只诊断，不会强套旧修复；三个 Legacy Adapter 继续 hash-lock。程序不自动 DDU，也不会为了“修黑屏”修改 BIOS 或 TDR 调试注册表。
