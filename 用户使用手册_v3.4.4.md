# Windows Game Runtime / ASUS Armoury Self-Healing Center v3.4.4 使用说明

正常使用只需要解压完整 ZIP，然后双击 `Launch-Win11.cmd`。

首次构建会先做静态安全检查。如果电脑没有 .NET 10 SDK，程序会自动处理：

1. 先尝试刷新 WinGet 的 `winget` source 并安装 `Microsoft.DotNet.SDK.10`。
2. 如果 WinGet 缺源、损坏、不可用或安装后仍检测不到 SDK，会自动切换到 Microsoft 官方 `dotnet-install.ps1`。
3. 官方脚本把 SDK 放到 `%LOCALAPPDATA%\WindowsGameRuntimeASUSSelfHealing\dotnet10`。
4. 该目录只作为本工具的构建 SDK 缓存，不要求重启/重新登录，也不会删除或替换机器已有的 .NET。

如果仍失败，请发送 `BuildLogs` 目录最新的：
- `WinUI3_OneClick_*.log`
- `StaticTests_*.log`
- 如果已进入 dotnet restore/publish，再附上相应构建输出。
