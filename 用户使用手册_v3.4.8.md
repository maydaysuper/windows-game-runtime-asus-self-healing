# v3.4.8 用户使用方式

v3.4.8 修复了 v3.4.7 源码在 Windows 11 上 `dotnet publish` 直接失败、无法得到可运行程序的问题。诊断/修复能力与 v3.4.7 相同。

如果手里只有源码包（没有 Setup / Portable）：

1. 完整解压 `Windows_Game_Runtime_ASUS_SelfHealing_WinUI3_v3.4.8_ReleaseKit.zip`。
2. 双击 `一键构建并启动_Win11.cmd` 或 `Launch-Win11.cmd`。
3. 静态门禁通过后会发布自包含 WinUI 3 目录并启动。不要只复制主 EXE，`Backend` 必须一起带走。

如果已有正式安装包，优先用：

- `Windows_Game_Runtime_ASUS_SelfHealing_Setup_v3.4.8_x64.exe`
- `Windows_Game_Runtime_ASUS_SelfHealing_Portable_v3.4.8_win-x64.zip`

Setup 按当前用户安装，不需要管理员权限；只有真正执行 ASUS、运行库或受允许的 GPU 修复时，Elevated Broker 才会弹 UAC。
